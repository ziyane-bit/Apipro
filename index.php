<?php
require_once 'ClassAutoLoad.php';

// call the method myMethod
$layoutsInstance->heading($conf);
$layoutsInstance->welcome($conf);
// call the signup_form method
$formInstance->signup();
$instanceInstance->footer($conf);